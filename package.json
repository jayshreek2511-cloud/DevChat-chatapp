{
  "name": "devchat",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time developer chat app with a Vercel-ready demo deployment.",
  "scripts": {
    "build": "npm run build --prefix client",
    "vercel-build": "npm install --prefix client && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "dev": "npm run dev --prefix server"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "uuid": "^9.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
